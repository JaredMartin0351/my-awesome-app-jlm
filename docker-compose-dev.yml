



services:
  web:
    container_name: my-awesome-app-test-jlm-dev

    build:
      context: ./
      dockerfile: ./Dockerfile-dev
    ports:
      - '3001:3000'
    volumes:
      - ./:/app
      - ./node_modules:/app/node_modules

  test:
    container_name: my-awesome-app-test-jlm
    depends_on:
      - "web"
    image: my-awesome-app-test-jlm
    volumes:
      - .:/app
      - ./node_modules:/app/node_modules
    command: ["npm", "test"]